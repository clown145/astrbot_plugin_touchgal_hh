{
    "touchgal_domain": {
        "description": "TouchGal 网站域名",
        "type": "string",
        "hint": "TouchGal 网站的主域名（不含 https:// 前缀）。当网站更换域名时可在此修改。",
        "default": "www.touchgal.top"
    },
    "shionlib_domain": {
        "description": "书音的图书馆 (Shionlib) 网站域名",
        "type": "string",
        "hint": "Shionlib 网站的主域名（不含 https:// 前缀）。当网站更换域名时可在此修改。",
        "default": "shionlib.com"
    },
    "shionlib_enabled": {
        "description": "启用 Shionlib 推荐",
        "type": "bool",
        "hint": "开启后，搜索结果会在合并消息开头显示 Shionlib 上的相关游戏链接。",
        "default": true
    },
    "shionlib_limit": {
        "description": "Shionlib 推荐数量",
        "type": "int",
        "hint": "返回的 Shionlib 相关游戏链接数量（每个链接单独一条消息）。",
        "default": 1
    },
    "show_nsfw": {
        "description": "显示 NSFW 内容",
        "type": "bool",
        "hint": "开启后，搜索结果将包含 NSFW（成人向）内容。",
        "obvious_hint": true,
        "default": false
    },
    "session_timeout": {
        "description": "搜索会话的超时时间（秒）",
        "type": "int",
        "hint": "用户发起搜索后，若在此时间内无后续操作（如翻页、选择），会话将自动结束。",
        "default": 60
    },
    "auto_search_enabled": {
        "description": "启用自动搜索功能",
        "type": "bool",
        "hint": "开启后，当群聊中检测到类似「有没有XXX资源」的消息时，将自动进行搜索并返回第一个结果的资源链接（以合并转发消息形式发送）。",
        "obvious_hint": true,
        "default": false
    },
    "auto_search_suggest_limit": {
        "description": "自动搜索推荐数量",
        "type": "int",
        "hint": "自动搜索时，在资源链接前显示的 TouchGal 相关游戏页面链接数量。",
        "default": 5
    },
    "auto_search_shionlib": {
        "description": "自动搜索时同时搜索书音",
        "type": "bool",
        "hint": "开启后，自动搜索时会同时搜索书音的图书馆，利用其模糊搜索能力。",
        "default": true
    },
    "auto_search_silent": {
        "description": "静默模式",
        "type": "bool",
        "hint": "开启后，自动搜索时不会发送「正在搜索」等提示词，搜不到也不会回复，只有找到资源时才会发送合并转发消息。",
        "default": true
    },
    "auto_search_pattern": {
        "description": "自动搜索的正则匹配模式",
        "type": "string",
        "hint": "用于匹配群聊消息并提取搜索关键词的正则表达式。第一个捕获组将作为搜索关键词。",
        "default": "(?:大佬|老哥|老师|朋友|大佬们)?\\s*(?:请问|问一下)?\\s*(?:有没有|求|群里有|找不到|大伙有|你们有|兄弟们有|谁有|哪里有|有人有)(?:一个|一下|一份|个)?\\s*[《【\\\"']?(.+?)(?:[》】\\\"']|(?:的)?(?:资源|游戏|下载|链接|安装包)|[吗呀啊呢么？?！!，,。.~～]|$)"
    }
}